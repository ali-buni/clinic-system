# 23 - AI

AI-powered appointment assistance, chat, and report summarization.

**Test Cases:** 14 | **Endpoint Folder:** [`endpoint/ai/`](./endpoint/ai/README.md)

---

## Table of Contents

| # | Method | Endpoint | Description | Auth |
|---|--------|----------|-------------|------|
| 1 | POST | [`/ai/appointment/assist`](#post-assist) | AI appointment assistance | Required |
| 2 | POST | [`/ai/chat/patient`](#post-chat) | AI chat for patient | Patient |
| 3 | POST | [`/ai/report/summarize`](#post-summarize) | AI report summarization | Required |

---

## POST `/ai/appointment/assist`

AI-powered appointment assistance. Auth required.

### Test Cases

#### `assist-doctor.json` — Doctor Request (422)
> [View JSON](./endpoint/ai/assist/assist-doctor.json)

**Request:**
```json
{ "message": "Show my schedule" }
```

**Response (422):**
```json
{ "message": "Provide a query (symptoms/specialty/doctor), specialty_id, or doctor_id to proceed.", "errors": { "input": ["Provide a query (symptoms/specialty/doctor), specialty_id, or doctor_id to proceed."] } }
```

---

#### `assist-patient.json` — Patient Request (422)
> [View JSON](./endpoint/ai/assist/assist-patient.json)

**Response (422):**
```json
{ "message": "Provide a query (symptoms/specialty/doctor), specialty_id, or doctor_id to proceed.", "errors": { "input": ["Provide a query (symptoms/specialty/doctor), specialty_id, or doctor_id to proceed."] } }
```

---

#### `assist-secretary.json` — Secretary Request (422)
> [View JSON](./endpoint/ai/assist/assist-secretary.json)

**Response (422):**
```json
{ "message": "Provide a query (symptoms/specialty/doctor), specialty_id, or doctor_id to proceed.", "errors": { "input": ["Provide a query (symptoms/specialty/doctor), specialty_id, or doctor_id to proceed."] } }
```

---

#### `assist-unauthenticated.json` — Unauthenticated (401)
> [View JSON](./endpoint/ai/assist/assist-unauthenticated.json)

**Response (401):** `{ "message": "Unauthenticated." }`

---

#### `assist-validation.json` — Validation Error (422)
> [View JSON](./endpoint/ai/assist/assist-validation.json)

**Request:** `{}` (empty)

**Response (422):**
```json
{ "message": "The input field is required.", "errors": { "input": ["The input field is required."] } }
```

---

## POST `/ai/chat/patient`

AI chat for patient queries. Patient only.

### Test Cases

#### `chat-patient.json` — Validation Error (422)
> [View JSON](./endpoint/ai/chat/chat-patient.json)

**Request:**
```json
{ "message": "Hello, I have a question" }
```

**Response (422):**
```json
{ "message": "The patient id field is required. (and 1 more error)", "errors": { "patient_id": ["The patient id field is required.", "You can only access your own patient data."] } }
```

---

#### `chat-history-patient.json` — Chat History (200)
> [View JSON](./endpoint/ai/chat/chat-history-patient.json)

**Response (200):**
```json
{ "success": true, "message": "Chat history retrieved", "data": [...] }
```

---

#### `chat-history-unauthenticated.json` — Unauthenticated (401)
> [View JSON](./endpoint/ai/chat/chat-history-unauthenticated.json)

**Response (401):** `{ "message": "Unauthenticated." }`

---

#### `chat-unauthenticated.json` — Unauthenticated (401)
> [View JSON](./endpoint/ai/chat/chat-unauthenticated.json)

**Response (401):** `{ "message": "Unauthenticated." }`

---

#### `chat-unauthorized-doctor.json` — Forbidden (403)
> [View JSON](./endpoint/ai/chat/chat-unauthorized-doctor.json)

**Response (403):** `{ "success": false, "message": "Permission Denied" }`

---

## POST `/ai/report/summarize`

AI-powered report summarization. Auth required.

### Test Cases

#### `summarize-doctor.json` — Doctor Request (422)
> [View JSON](./endpoint/ai/summarize/summarize-doctor.json)

**Request:**
```json
{ "report_id": 4 }
```

**Response (422):**
```json
{ "message": "The record id field is required.", "errors": { "record_id": ["The record id field is required."] } }
```

---

#### `summarize-patient.json` — Patient Request (422)
> [View JSON](./endpoint/ai/summarize/summarize-patient.json)

**Response (422):**
```json
{ "message": "The record id field is required.", "errors": { "record_id": ["The record id field is required."] } }
```

---

#### `summarize-unauthenticated.json` — Unauthenticated (401)
> [View JSON](./endpoint/ai/summarize/summarize-unauthenticated.json)

**Response (401):** `{ "message": "Unauthenticated." }`

---

#### `summarize-validation.json` — Validation Error (422)
> [View JSON](./endpoint/ai/summarize/summarize-validation.json)

**Request:** `{}` (empty)

**Response (422):**
```json
{ "message": "The record id field is required.", "errors": { "record_id": ["The record id field is required."] } }
```

---

[Back to Index](./00-INDEX.md)
